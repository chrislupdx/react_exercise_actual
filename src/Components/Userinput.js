import React from 'react';

const userinput = () => {
    return (
    <div>
        <input type="submit" value="Submit" />
        <input type="text" />
    </div>

    );
};

export default userinput;